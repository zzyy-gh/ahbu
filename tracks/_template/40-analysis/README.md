# Track layer 40 — Analysis & Writeup

**Mandate.** Interpret this track's results, situate them honestly relative to prior work, communicate findings. A null or partially-null result is a valid outcome — write it as a finding, not as a failure. May retire-cancel the track if the result has no informative direction (e.g., underpowered to discriminate, dataset turned out misaligned with the headline question); document the cancel reason and the lessons that survive.

**Knowledge.** Results from track layer 30, methodology from track layer 20, the admission record, vision, prior literature.

**Output.**
- `findings.md` — what we learned. What worked, what didn't, what surprised.
- `limitations.md` — honest limitations. What the result does *not* show.
- `next.md` — follow-ons worth pursuing under the same hard-constraint discipline.
- `lessons.md` (also at track root) — what generalises across tracks; candidates for `shared/` promotion.

**Help target.** Track layer 30 (Experiments).

---

Critic pass before sign-off. Then update `layers/10-pain-point-validation/portfolio.md` to `retire-completed` or `retire-cancelled` (with reason), surface lessons to `shared/` or `lessons.md`.
